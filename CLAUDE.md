# Puckmite — 작업 지침 (CLAUDE.md)

Unity 2D 게임 프로젝트. 아래 규칙은 이 저장소에서 작업할 때 항상 지킨다.

## 게임 개요

**Puckmite**. 탑다운 턴제 전술 게임. 납작한 원반(퍽)을 컬링처럼 밀어 보드에 올린다. 쏜 퍽은 사라지지 않고 보드에 남아 칸을 점유하고 싸운다.

## 핵심 규칙

- 격자 보드. 칸마다 효과가 있고, 퍽이 여러 칸에 걸치면 효과를 모두 받는다.
- 턴제 교대. 플레이어와 적이 번갈아 퍽을 민다.
- 퍽끼리 충돌하면 양쪽 체력이 깎인다. 체력은 3~5의 작은 정수.
- 벽에 튕기면 경험치를 얻어 레벨업. 레벨은 매치마다 초기화.
- 파워가 셀수록 착지 범위가 넓어진다. 성장과 정확도가 트레이드오프.

## 아키텍처 원칙

- **물리는 직접 구현한다.** `Rigidbody2D`와 `Collider2D`를 게임 로직에 쓰지 않는다.
- **시뮬레이션과 렌더링을 분리한다.** 시뮬은 순수 C#, MonoBehaviour는 시뮬을 읽어서 그리기만 한다.
- **시뮬레이션은 결정론적이어야 한다.** 같은 입력은 항상 같은 결과.
- **고정 타임스텝을 쓴다.** `Time.deltaTime`을 시뮬레이션에 넣지 않는다.
- 이유: 착지 미리보기와 적 AI가 같은 시뮬을 화면 없이 수백 번 돌려야 한다.

## 프로젝트 스택 (검증된 값)

- **Unity 버전**: `6000.5.3f1` (Unity 6)
- **차원**: 2D
- **렌더 파이프라인**: URP `17.6.0` (`com.unity.render-pipelines.universal`), 2D Renderer 사용 — `Assets/Settings/Renderer2D.asset`
- **입력**: 새 Input System `1.19.0` (`com.unity.inputsystem`) — 액션 애셋 `Assets/Settings/InputSystem_Actions.inputactions`. 레거시 Input Manager 아님
- **2D 패키지**: 2D Animation, Aseprite, PSD Importer, SpriteShape, Tilemap(+Extras)

> 익숙한 패턴을 복사하기 전에 이 스택을 전제로 한다. 세상의 대부분 Unity 코드는 Built-in + 레거시 입력을 가정하므로 그대로 가져오지 말 것.

## 출력 언어 — 사용자는 한국어로 읽는다

세션에서 사용자가 읽는 모든 텍스트는 한국어로 쓴다: 채팅 응답, 도구 호출 위 진행 안내 한 줄, 도구 `description` 필드, 서브에이전트 프롬프트/설명, 계획 텍스트, todo, (요청 시) 커밋 메시지 본문.

단, 다음은 한국어 문장 안에 원문 그대로 둔다: 식별자, 파일 경로, 명령어, API/패키지 이름, 그대로 인용하는 에러 출력.

## 검증 — 실행하지 않았으면 했다고 말하지 않는다

- 실제로 실행하지 않았다면 "동작한다 / 컴파일된다 / 테스트 통과 / 고쳤다"고 말하지 않는다. 무엇을 바꿨고 사용자가 정확히 무엇을 확인해야 하는지 전달한다.
- 명령을 돌린 것 = 검증이 아니다. 출력이 곧 성공의 증거가 아니다(에러 메시지도 출력이다). 돌아온 내용을 읽고, 종료 코드를 확인하고, 그게 무엇을 증명하는지 판단한 **뒤** 보고한다.
- 테스트한 범위만 정확히 주장한다. "파일이 있다" ≠ "동작한다". "컴파일된다" ≠ "올바르게 동작한다". 확인이 약했으면 약했다고 말한다.
- 상태를 만들어냈을 코드로부터 상태를 추론하지 않는다 — 실제 상태(파일, 씬, 출력)를 읽는다.
- 방금 내가 한 수정도 현재 상태의 일부다. 건드리지 않은 것처럼 추론하지 않는다.
- 주장이 틀린 것으로 드러나면 즉시, 명확히 정정한다: 무엇을 주장했고, 실제로 무엇이 맞고, 그게 틀렸다는 것.

## 작업 범위 — 요청 이상으로 행동하지 않는다

- 요청하지 않은 기능을 추가하지 않는다.
- UI 꾸미기, 이펙트, 사운드는 지시 전까지 만들지 않는다.
- 구조를 바꾸는 변경은 계획을 먼저 제시한다.
- 작업에 필요하지 않은 도구/실행 파일을 돌리지 않는다. "그냥 확인차"는 이유가 안 된다 — 낯선 프로그램은 부작용(대화상자, 파일 생성, 상태 변경)이 있다.
- 범위를 벗어난 제안은 한 번만 한다. 사용자가 받아들이지 않으면 그만둔다. 반복은 도움이 아니라 압박이다.
- 요청이 열려 있으면 추측하지 말고 묻는다. 질문 하나가 잘못 만드는 것보다 싸다.

## 코딩 방식 — 생각하고, 단순하게, 외과적으로

아래는 속도보다 신중함으로 기운다. 사소한 작업엔 판단을 쓴다.

**짜기 전에 생각한다.**
- 가정을 명시한다. 불확실하면 묻는다.
- 해석이 여러 개면 조용히 하나 고르지 말고 제시한다.
- 더 간단한 방법이 있으면 말하고, 근거가 있으면 반대한다.
- 불분명하면 멈춰서 무엇이 헷갈리는지 짚고 묻는다.

**단순함이 먼저.** 문제를 푸는 최소 코드. 투기적인 것 금지.
- 일회용 코드에 추상화를 만들지 않는다.
- 요청 안 한 "유연성"·"설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황을 위한 에러 처리를 넣지 않는다.
- 200줄인데 50줄로 되면 다시 쓴다. "시니어가 과설계라 하겠는가?"에 그렇다면 단순화한다.
- (요청 범위 밖 기능을 만들지 않는 것은 위 "작업 범위" 참고.)

**외과적 변경.** 꼭 건드려야 할 것만 건드리고, 내가 만든 것만 치운다.
- 인접한 코드·주석·포맷을 "개선"하지 않는다. 안 깨진 걸 리팩터링하지 않는다.
- 다르게 짰을 것 같아도 기존 스타일에 맞춘다.
- 관련 없는 죽은 코드는 발견하면 지우지 말고 언급만 한다.
- 내 변경이 만든 고아(import·변수·함수)만 제거한다. 기존의 죽은 코드는 요청 없이 지우지 않는다.
- 기준: 바뀐 모든 줄이 사용자 요청으로 직접 추적돼야 한다.

**목표 기반 실행.** 성공 기준을 먼저 정하고 검증될 때까지 반복한다.
- 모호한 작업을 검증 가능한 목표로 바꾼다: "검증 추가"→잘못된 입력 테스트를 쓰고 통과시킨다, "버그 수정"→재현 테스트를 쓰고 통과시킨다, "리팩터"→전후로 테스트가 통과하는지 확인한다.
- 다단계 작업은 짧은 계획을 먼저 낸다: 각 단계 → 검증 방법.
- 단, 이 환경에서는 컴파일·Play·테스트 실행이 안 된다(위 "검증", "Unity — C# 컨벤션" 참고). 성공 기준은 그대로 정의하되, 여기서 돌릴 수 없는 검증은 "어느 씬에서 무엇을 눌러 무엇을 확인하는지"로 구체적으로 넘긴다.

## 사실과 API — 기억이 아니라 검증

- 빠르게 변하는 API를 기억으로 쓰지 않는다. 실제 설치된 것과 대조하거나 웹을 검색한다. 학습 데이터는 달력보다 몇 달 뒤처진다.
- 심볼이 존재한다고 최신인 것은 아니다 — 폐기된 이름도 여전히 컴파일되고 경고로만 실패한다. 같은 일을 하는 두 이름이 있으면 보통 새 쪽이 살아 있는 쪽이다.

## 공통 하드 규칙

- 컴파일러/린터 경고를 없애려고 **억제하지 않는다** — 원인을 고친다.
- 생성/빌드 폴더는 편집·삭제하지 않는다. 읽기는 괜찮다.
- 요청받기 전에는 커밋/푸시하지 않는다.
- 포맷이나 도구가 직접 편집을 막으면, 사용자에게 수동 클릭 체크리스트를 넘기지 말고 적절한 API/스크립트로 자동화한다.

## Unity — 하드 규칙

1. Unity YAML을 손으로 편집하지 않는다 (`.unity`, `.prefab`, `.asset`, `.inputactions`, `.scenetemplate`). 에디터가 관리하는 포맷이라 텍스트 편집은 파일을 깨뜨린다. 대신 Editor 스크립트를 짠다.
2. `.meta` 파일을 만들거나 편집하거나 삭제하지 않는다. 그 GUID가 모든 애셋 참조의 대상이다. Unity가 생성한다. 스크립트를 옮기거나 이름을 바꾸면 `.meta`도 함께 가야 하므로 — 에디터에서 사용자가 직접 하게 두는 편이 낫다.
3. 생성 폴더는 건드리지 않는다: `Library/`, `Temp/`, `obj/`, `Logs/`, `UserSettings/`. (API 확인용으로 `Library/PackageCache/`를 읽는 것은 괜찮다.)
4. 익숙한 패턴을 복사하기 전에 설치된 셋업을 확인한다 — 렌더 파이프라인, 입력, 차원, Unity 버전. (이 프로젝트 값은 위 "프로젝트 스택" 참고.)

## Unity — 씬/오브젝트 셋업은 자동화, 수동 체크리스트 금지

하드 규칙 1이 씬을 텍스트로 편집하는 것을 금지한다 — 답은 인스펙터 클릭 체크리스트가 **아니다**. Editor 스크립트를 짜면 Unity 자체 API를 거치므로 YAML을 깨뜨리지도, 작업을 사용자에게 떠넘기지도 않는다.

- 먼저 기존 `Assets/Editor/` 스크립트를 읽고 재사용한다 (`EditorApplication.ExecuteMenuItem("Tools/…")`). 처음부터 다시 구현하지 않는다 — 그 스크립트들은 새로 짜면 틀리기 쉬운 프로젝트 사실을 담고 있다.
- GameObject·컴포넌트·콜라이더·Build Settings 항목을 만들거나 설정하는 일은 `Assets/Editor/` 스크립트의 `[MenuItem]` (경로 `Tools/`) 뒤에 둔다. 최상위 커스텀 메뉴는 추가하지 않는다.
- private `[SerializeField]` 필드는 `SerializedObject.FindProperty`로 설정한다.
- 멱등하게 만든다 — 중복 생성 대신 기존 오브젝트를 찾아 재설정해서 재실행이 안전하게 한다.
- 메뉴 클릭과 Play 누르기는 사용자 몫이다 — 그 부분은 여기서 자동화할 수 없다.

## Unity — API는 설치된 것과 대조해 검증

Unity는 학습 데이터가 따라잡는 속도보다 빠르게 API를 폐기하고, 여기서는 컴파일할 수 없으므로, 잘못 추측하면 사용자가 경고로 발견하게 된다.

- **패키지 API**: `Library/PackageCache/<pkg>@<hash>/` 소스를 읽는다 — 설치된 정확한 버전이 시그니처·기본값·네임스페이스를 한 번에 확정한다.
- **엔진 API** (`UnityEngine.*`): 멤버 이름은 `<UnityInstall>/Editor/Data/Managed/UnityEngine/*.dll` 메타데이터에 있다. 단순 문자열 스캔으로 심볼 존재를 확인한다. (`ReflectionOnlyLoadFrom`은 실패한다 — netstandard 파사드가 해석되지 않는다.)
- 심볼이 존재하는지뿐 아니라 정확한 형태(네임스페이스, 중첩, 전체 시그니처)까지 맞는지 확인한다.

## Unity — C# 컨벤션

- **검증**: 여기서는 컴파일·Play 모드 진입·테스트 실행이 안 된다. 컴파일된다/동작한다/테스트됐다고 주장하지 않는다. 구체적으로 넘긴다: 어느 씬에서, 무엇을 누르고, 무엇이 일어나야 하는지.
- `GetComponent`/`Find`/`transform.GetChild`보다 `[SerializeField] private` 참조 — 인스펙터에서 연결한다.
- `Update`/`FixedUpdate` 안에서 `GetComponent`/`Find*`/할당하는 LINQ를 호출하지 않는다. `Awake`에서 캐싱한다.
- `GetComponent` + null 체크보다 `TryGetComponent`.
- 이벤트는 `= delegate { }`로 초기화해 호출부에서 null 체크가 필요 없게 한다.
- null 체크 실패가 게임을 깨뜨린다면 `Debug.LogError` — 조용히 실패하지 않는다.
- 파일당 MonoBehaviour 하나, 파일명은 클래스명과 정확히 일치 (Unity 요구사항).
- 언어는 파일이 아니라 독자로 나뉜다: 코드·주석·`Debug.Log`/`LogError` 출력은 영어(개발자가 읽는다), 게임 내 UI 텍스트는 한국어(플레이어가 읽는다). 사용자가 한국어로 대화한다는 것이 소스 안 문자열을 한국어로 쓰라는 신호는 아니다.

| 종류 | 스타일 | 예시 |
|---|---|---|
| 클래스, 메서드, 프로퍼티, 이벤트, const, enum | PascalCase | `IceCreamMachine`, `Serve()`, `MaxScoops` |
| private / protected 필드 | `_camelCase` | `_scoopCount` |
| public 필드 | PascalCase | `ScoopCount` — 단 프로퍼티나 `[SerializeField] private`를 우선 |
| 지역 변수, 파라미터 | camelCase | `scoopCount` |
| 인터페이스 | `I` + PascalCase | `IFlavorSource` |
| Boolean 메서드 / 프로퍼티 | Is / Has / Can | `IsMelting`, `HasCone()` |
| 이벤트 핸들러 | `On` + 이벤트 | `OnScoopAdded` |

## 폴더 구조

`Assets/Scripts/`는 시뮬과 뷰로 최상위 분리한다 (위 "아키텍처 원칙" 참고):

- `Assets/Scripts/Sim/` — 시뮬레이션. `UnityEngine` 의존은 `Vector2`만. `Rigidbody2D`/`Collider2D`/MonoBehaviour 금지.
- `Assets/Scripts/View/` — 렌더링, 입력. MonoBehaviour는 여기 둔다.

나머지는 `Assets/` 아래 종류별로 정리: `Scenes/`, `Prefabs/`, `Art/`(`Sprites/`, `Animations/`, `Materials/`), `Audio/`, `UI/`, `Editor/`(Unity 특수 폴더 — 이름 바꾸지 말 것), `Settings/`(Unity 생성 — 재정리하지 말 것), `Plugins/`(서드파티).

- 폴더는 첫 파일이 생길 때 만든다. 빈 폴더 금지.
- 서드파티 임포트는 위 폴더들 밖으로 둔다.
- 파일·폴더명은 PascalCase, 공백 없음.
