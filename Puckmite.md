# Puckmite 설계 문서

가제 `Puckmite` (퍽마이트). puck + mite.
검색 결과가 존재하지 않는 것을 확인함.

---

## 이 문서를 읽는 법

세 가지로 나눠서 적었다.

- **확정** — 정한 것. 근거가 되는 결정이 실제로 내려진 항목.
- **미정** — 아직 안 정한 것. 여기 있는 건 구현하면 안 된다.
- **보류** — 초기 구상에는 있었으나 현재 설계에 포함되지 않은 것.

수치가 안 적혀 있으면 안 정한 것이다. 임의로 채우지 말 것.

---

# 1. 개요

탑다운 턴제 전술 게임. 납작한 원반(스톤)을 격자 보드 위에서 **당구처럼** 친다.
스톤은 굴린 뒤에도 사라지지 않고 보드에 남아 칸을 점유한다.

화면 위쪽에서는 플레이어 캐릭터와 적 캐릭터가 싸우고,
아래쪽 보드는 그 싸움에 쓰일 공격력과 방어력을 만들어내는 곳이다.

한 줄 정체성: **쏜 것이 사라지지 않는다. 그 자리에 남아 힘이 된다.**

## 1.1 핵심 동사 (확정)

- 보드 위에 이미 있는 **자기 스톤을 그 자리에서 친다**. 큐볼과 같다
- 칠 수 있는 것은 **자기 스톤뿐**이다. 상대 스톤은 직접 칠 수 없다
- 친 스톤이 다른 스톤을 밀어내고, 밀려난 스톤이 또 다른 스톤을 민다.
  **한 발이 판 전체를 재배치한다**
- 새 스톤을 보드에 투입하는 것은 손에 스톤이 남아 있을 때뿐이다.
  스톤 2개로 시작하므로, 대부분의 턴은 큐샷이다

## 1.2 한 발에 노릴 수 있는 것 (확정)

보드는 양쪽이 공유하는 전장이다. 한 샷으로 다음을 동시에 노린다.

- 자기 스톤을 좋은 버프칸에 세운다
- 상대 스톤을 버프칸에서 걷어내 바깥 데미지 칸으로 보낸다
- 상대 스톤에 부딪혀 체력을 깎는다 (파괴 노림)
- 벽에 튕겨 경험치를 번다

---

# 2. 진행 구조

## 2.1 확정

- 총 **3스테이지**
- 한 스테이지는 **5번의 런**으로 구성. 5번을 모두 클리어해야 스테이지 클리어
- 각 런의 적 수는 순서대로 **1, 1, 2, 3, 보스**
- 보스 런에는 **강한 보스 1마리만** 등장한다
- 각 런에 어떤 적이 나오는지는 **고정**이다 (랜덤 아님)
- 한 런이 끝나면 **곧바로 상점**이 열린다
- 상점은 건너뛰거나 잠글 수 없다
- 런이 끝나면 플레이어 체력이 **일정량 회복**된다 (회복량은 상점에서 업그레이드 가능)
- **스테이지가 바뀔 때 추가 회복은 없다**
- 게임을 켜면 곧바로 게임이 시작된다 (메인 메뉴 없음)
- 이어하기 없음
- 죽으면 **1스테이지부터 완전히 다시 시작**한다. 영구 해금 요소 없음

## 2.2 화면 구성 (확정)

- 위쪽: 플레이어 캐릭터와 적 캐릭터. 각각 공격력, 방어력, 체력이 보인다
- 아래쪽: 보드가 크게 자리한다
- Peglin과 유사한 레이아웃

---

# 3. 전투

## 3.1 보드 (확정)

- **5x5, 총 25칸**
- 안쪽 **3x3(9칸)** 이 버프칸. 공격력 칸과 방어력 칸이 있다
- 버프칸은 **안쪽으로 갈수록 효과가 강해진다**
- 바깥 **16칸**은 버프를 주지 않으며, **데미지 칸**이다. 16칸 모두 동일하다
- 데미지 칸은 그 칸에 있는 **모든 스톤**의 체력을 깎는다 (아군·적군 무관)
- 보드 가장자리에는 **벽**이 있다. 스톤은 튕겨서 보드 안으로 돌아온다
- 벽에 반사될 때 **약간의 감속**이 있다
- 칸 한 변의 길이를 5로 볼 때 **스톤의 지름은 3**
- 나중에 보스 패턴이나 스테이지 진행에 따라 칸 배치를 변경할 수 있다

## 3.2 칸 점유 판정 (확정)

- 스톤이 **겹친 칸 전부**를 점유한다. 4칸 교차점에 서면 4칸을 동시에 받는다
- 겹친 칸의 효과는 **각각 100%씩 전부 적용**된다. 면적 비례 아님
- **한 칸에 스톤 두 개가 멈추면 둘 다 버프를 받는다**
- 버프칸과 데미지 칸에 동시에 걸치면 **둘 다 적용**된다
- 데미지 칸 여러 개에 걸치면 **그만큼 여러 번** 데미지를 받는다
- 데미지로 체력이 0이 되더라도 **버프 효과를 먼저 적용받고 사라진다**
- 겹침에는 **최소 임계값**을 둔다. 눈으로 걸쳤는지 아닌지 판별 가능한 정도여야 한다
  - 임시값: 스톤이 칸 경계를 **0.1 이상** 넘었을 때 점유로 친다 (초안의 0.3에서 손맛 확인 뒤 낮췄다)
  - **스톤 반지름(1.5) 이상으로 올리면 안 된다.** 판정식이 `반지름 - 최근접거리 ≥ 임계값`이라
    반지름 이상이면 어떤 칸도 점유로 잡히지 않아 칸 하이라이트·버프·데미지 칸이 한꺼번에 사라진다
  - 구현에서는 튜닝 슬라이더가 아니라 **상수**로 둔다 (실수로 위 구간에 들어가는 사고가 실제로 있었다)

## 3.3 스톤 (확정)

- 플레이어는 스톤 **2개**로 시작한다
- 스톤 보유 개수에 **상한은 없다**. 물리적으로 자리가 부족해지면 보드를 키우거나 스톤 크기를 줄인다
- 적은 종류마다 스톤 보유 개수가 **다르다**. 보스는 일단 **3개**
- 스톤에는 체력이 있다
- 체력이 0이 되면 스톤은 **즉시 파괴**된다. 굴러가는 도중이라도 그 자리에서 사라진다
  - 단, **충돌 물리는 정상 적용**된다. 상대 스톤은 부딪힌 만큼 튕겨 나간다
- 파괴된 스톤은 **레벨과 경험치가 초기화된 새 스톤**이 되어 **즉시 손으로 돌아온다**.
  다음 턴부터 사용 가능하다
- **아군 스톤끼리 충돌해도 체력은 깎이지 않는다**. 경험치만 얻는다
- **적 스톤과 충돌하면 서로 1씩** 깎인다
- 적 스톤과의 충돌 피해는 나중에 강화로 막을 수 있다
- 적이 죽으면 그 적의 스톤은 보드에서 사라진다
- **적끼리는 아군 취급**이다. 적 A의 스톤과 적 B의 스톤이 부딪혀도 체력은 깎이지 않는다
- 적 스톤도 파괴되면 레벨 초기화된 새 스톤으로 그 적에게 돌아간다. **플레이어와 규칙이 완전히 동일하다**

## 3.4 스톤 진입 (확정)

- 플레이어의 새 스톤은 **왼쪽 변**, 적의 새 스톤은 **오른쪽 변**에서 나온다
- 변에 **붙은 상태**로 나온다
- 진입 위치는 그 변을 따라 이동할 수 있으나, **다른 변으로는 옮길 수 없다**
- 진입 변은 그 자체가 데미지 칸 열이다. **의도된 압력이다.**
  새로 놓은 스톤을 안쪽으로 보내지 못하면 곧바로 깎이기 시작한다

## 3.5 턴 (확정)

### 턴 순서

`플레이어 → 적1 → 적2 → 적3` 의 **개별 턴**이다.
각자 자기 턴에 굴리고 공격한다.
적이 중간에 죽으면 남은 적으로 순서가 당겨진다.

### 한 턴의 시퀀스

1. **데미지 칸 정산** — 그 시점에 데미지 칸에 있는 자기 스톤이 체력을 잃는다 (턴 시작)
2. **굴리기** — 스톤 하나를 굴린다. **강제**다. 굴리지 않고 넘어갈 수 없다
3. **효과 적용** — 모든 스톤이 멈춘 뒤, 점유 칸을 기준으로 능력치를 계산한다
4. **공격** — 대상을 골라 공격한다. **강제**다. 1턴 1회, 대상 1명

### 굴리기 규칙

- 한 턴에 **스톤 하나만** 굴릴 수 있고, 굴리면 그 턴의 굴리기는 끝난다
- 굴릴 스톤은 두 가지 중에서 고른다
  - **새 스톤** — 손에 남은 스톤이 있을 때만 선택 가능. 진입 변에서 출발한다
  - **보드 위의 자기 스톤** — 클릭해서 **그 자리에서** 친다 (큐샷)
- 굴린 스톤이 도중에 파괴되어도 그 턴은 계속된다. 남은 스톤들로 효과를 적용하고, 공격도 그대로 한다
- 스톤이 하나도 없으면 **기본 데미지로 공격**한다
- 스톤이 굴러가는 장면은 플레이어와 적 모두 **배속으로 넘길 수 있고, x1로 볼 수도 있다**

## 3.6 능력치 (확정)

### 공격력

- 플레이어와 적 모두 **기본 공격력**을 가진다
- 그 위에 **현재 점유한 칸의 버프**가 얹힌다
- **매 턴 다시 계산**된다. 누적되지 않는다. 스톤이 칸에서 벗어나면 그 효과는 사라진다

### 쉴드 (방어력)

방어력은 **일정 데미지를 막고 사라지는 쉴드**다. 두 종류가 따로 있다.

- **기본 쉴드**
  - **런이 시작할 때 초기화**된다
  - 런 도중에는 **다시 차지 않는다**. 한 번 깎이면 그 런 동안 돌아오지 않는다
- **효과 쉴드**
  - 점유한 칸의 효과가 적용될 때 **그 효과량만큼 리필**된다
  - **자기 턴이 다시 돌아올 때까지만 유지**된다. 넘겨서 쌓이지 않는다
  - 방어칸에 눌러앉아 무한히 쌓는 농성 전략을 막기 위한 규칙이다

- 쉴드 **상한은 없다**
- 데미지는 **쉴드를 먼저 소모하고**, 남은 만큼 체력을 깎는다
- 적도 동일한 규칙을 따른다

### 난이도에 대해

적이 3명일 때 플레이어는 한 번 공격하고 적은 세 번 공격한다. **의도된 난이도다.**

## 3.7 성장 (확정)

- 경험치는 두 가지 경로로 얻는다
  - **벽에 튕겼을 때**
  - **아군 스톤과 충돌했을 때**
- **벽 반사 경험치는 소유자와 무관하게 항상 적용**된다.
  내가 친 스톤이 상대 스톤을 밀어 벽에 맞혀도 **그 상대 스톤이 경험치를 얻는다**
- **충돌 세기와 무관하게 동일한 경험치**를 준다. 살짝 건드려도 같다.
  의도된 설계이며 최소 속도 조건을 두지 않는다
- 코너에 부딪히면 **변마다 1씩** 센다. 정확히 코너면 2다
- 레벨업에 필요한 경험치는 **3 고정**
- **레벨 상한은 5**. 만렙 이후의 경험치는 버려진다
- 레벨이 오르면 **그 스톤이 올라간 칸에서 받는 효과가 커진다**. **가산** 방식이다
- **레벨업 시 체력 회복은 없다**
- **런이 끝나면 레벨과 경험치는 초기화**된다. 다음 런은 처음부터다

## 3.8 승패 (확정)

- 적의 체력이 모두 닳으면 승리
- 플레이어의 체력이 모두 닳으면 게임 종료

---

# 4. 적

## 4.1 확정

- 처음에는 1명, 점점 늘어나 **최대 3명**
- **적 종류가 여러 개**이며, 종류마다 기본 공격력·기본 쉴드·체력·스톤 개수가 다르다
  - 예: 쉴드가 많고 데미지가 적은 적 / 데미지가 세고 체력이 낮은 적
- 적마다 스톤을 보유하며, **보유 개수를 머리 위에 표시**한다
- 특정 적에게 마우스를 올리면 **그 적의 스톤을 하이라이트 또는 깜빡임으로 표시**한다
- 적의 공격력과 쉴드도 **자기 스톤이 점유한 칸에서 나온다**. 플레이어와 동일한 규칙이다
- 보스는 체력이 많고 공격력이 세며 스톤을 많이 가진다
- **보스는 스테이지마다 패턴이 다르다**. 고유 행동과 칸 배치 변경을 포함한다 (세부는 미정)
- 스테이지가 올라가면 적 체력·공격력, 보드 배치, 적의 능력 등이 다양하게 바뀐다

---

# 5. 상점

## 5.1 구조 (확정)

- 런이 끝나면 곧바로 열린다. 건너뛸 수 없다
- **전용 5x5 보드**를 가진다. 전투 보드와 별개이며 칸 효과도 다르다
- 상점 보드에는 **데미지 칸이 없다**
- 상점 보드는 **처음에 모든 칸이 비어 있다**. 빈 칸에 스톤이 멈추면 아무 일도 없다
- 플레이어는 골드로 **칸을 사서 자유롭게 배치**한다. 25칸을 전부 채울 수 있다
- 배치한 칸은 **계속 유지**된다 (런과 스테이지를 넘어 지속)
- **한번 놓은 칸은 옮길 수 없다**
- 같은 종류의 칸을 겹쳐 올리면 **그 칸이 강화**된다
- **다른 종류를 겹치려 하면 경고**한다. 기존 강화 효과를 모두 잃을 수 있다는 안내 후,
  OK를 누르면 교체되고 취소를 누르면 그대로 둔다

## 5.2 상점 보드의 동사 (확정)

전투와 다르다. **큐샷이 없다.**

- 상점 스톤은 **매번 새로 던지기만** 가능하다. 진입 변은 전투와 같은 **왼쪽 변**
- 스톤끼리 **충돌한다**. 충돌하면 **경험치를 얻는다**
- **벽 반사 경험치도 있다**
- 점유 판정은 **전투와 동일**하다 (겹친 칸 전부)
- 스톤을 전부 던지고 나면, **최종 보드 상태의 효과를 합산**해서 받고 상점이 닫힌다
- 칸 구매와 배치를 **먼저 모두 끝낸 뒤** 스톤을 던진다

## 5.3 칸 제시 (확정)

- 현재 칸 종류는 **4종**
- 그중 **1개만 제시**한다
- **리롤**로 새 칸을 살 수 있다
- **리롤 가격은 리롤할수록 올라간다**
- **전투 스톤 구매도 같은 슬롯**을 통해 등장한다. 등장 확률은 낮다

## 5.4 상점 스톤 (확정)

- 상점마다 스톤 **1개가 기본 지급**된다
- 골드로 **추가 스톤을 구매**할 수 있다
- 추가 구매한 스톤은 **그 상점에서만** 쓸 수 있다. 다음 상점에서는 다시 1개로 돌아간다
- 상점 스톤도 레벨업하지만, **매 상점마다 1레벨부터** 다시 시작한다
- **전투 스톤 개수와 상점 스톤 개수는 완전히 별개**다

## 5.5 구매 가능한 강화 (확정)

칸 4종은 다음에 대응한다.

- **기본 공격력 증가**
- **기본 쉴드량 증가**
- **런 종료 시 회복량 증가**
- **최대 체력 증가**

강화는 **게임이 끝날 때까지 누적**된다.
이 강화는 상점 보드에 스톤을 굴려 칸 위에 올리는 방식으로 획득한다.

## 5.6 골드 (확정)

- 골드는 **적을 처치할 때마다** 얻는다
- 골드는 **상점 사이에 이월**된다
- 상점에서 산 전투 스톤은 **즉시** 손에 들어온다

---

# 6. 미리보기와 입력

## 6.1 조준 (확정)

- 스톤을 클릭한 뒤, **방향과 게이지를 동시에** 조절한다
- 게이지는 **드래그로 정하는 값**이다. 왕복하는 타이밍 게이지가 아니다.
  손기술이 아니라 판단으로 결정되는 게임이다

## 6.2 미리보기 (확정)

- **큐 스톤의 궤적만** 보여준다. 맞은 스톤의 궤적은 보여주지 않는다
- 큐 스톤이 **최종 정지하는 위치까지** 그린다. 큐가 스톤·벽에 튕기는 것은 반영한다
- **큐가 친 스톤은 미리보기에서 제거**해, 그 스톤이 되돌아와 큐를 다시 치는 캐스케이드는 반영하지 않는다 — 그런 샷에선 실제 결과와 다를 수 있다 (의도된 정보 부족)
- 큐의 **예상 최종 위치를 스톤 크기 마커**로 표시한다
- 파괴 여부는 **보여주지 않는다**

이 제한이 설계의 핵심이다.
**내 큐 스톤의 운명은 알 수 있지만, 그 스톤이 걷어찰 다른 스톤들의 결과는 알 수 없다.**
세게 치는 샷의 대가는 확률이 아니라 **정보 부족**에서 나온다.

## 6.3 스톤 표시 (확정)

- **체력** — 스톤 제일 겉면을 호(arc) 형태로 나눠 표시. 깎이면 호가 하나씩 줄어든다
- **레벨** — 스톤 중앙에 숫자
- **경험치** — 스톤 가운데를 아래에서 위로 채우는 게이지
- **소유자** — 색으로 구분. 아군/적군 2색만 쓰고, 적 개별 구분은 호버 하이라이트에 맡긴다

---

# 7. 구현 방침

## 7.1 물리 (확정)

- 물리는 **직접 구현**한다. 게임 로직에 `Rigidbody2D`와 `Collider2D`를 쓰지 않는다
- **시뮬레이션과 렌더링을 분리**한다. 시뮬은 순수 C#, MonoBehaviour는 결과를 읽어 그리기만 한다
- 시뮬레이션은 **결정론적**이어야 한다. 같은 입력은 항상 같은 결과를 낸다
- **고정 타임스텝 1/120**을 쓴다. `Time.deltaTime`을 시뮬레이션에 넣지 않는다
- 이유: 착지 미리보기와 적 AI가 같은 시뮬을 화면 없이 반복 실행해야 한다

## 7.2 시뮬이 들고 있어야 하는 것 (확정)

- **스톤의 체력은 Sim 안에 있어야 한다.**
  굴러가는 도중 파괴가 발생하고, 파괴 후에도 충돌 물리는 적용되어야 하기 때문이다
- **스톤의 소유자(Owner)** — 충돌 시 경험치인지 데미지인지 갈린다
- **BounceCount** — 벽 반사 경험치는 소유자 무관이므로 퍽별 카운트가 그대로 맞다

## 7.3 결정론 유지 규칙 (확정)

- 한 스텝에 여러 충돌이 발생할 때 **처리 순서를 Id 오름차순으로 고정**한다.
  리스트 인덱스 순회는 스톤 파괴·재생성으로 순서가 바뀌면 결과가 달라진다
- 겹침 해소(positional correction)도 같은 순서를 따른다
- `Simulate()`에 **최대 스텝 캡**을 둔다. 미세 진동으로 종료되지 않는 케이스를 막는다.
  AI가 헤드리스로 수백 번 돌리므로 무한 루프 한 번이면 에디터가 멈춘다

## 7.4 터널링 (확정)

- 보드는 25x25, 스톤 지름 3이다. dt=1/120에서 속도 360이면 스텝당 이동이 3이라
  **스톤이 다른 스톤을 통과**한다
- 스텝당 이동거리가 반지름을 넘으면 **서브스텝으로 분할**하거나,
  **파워 상한을 물리적으로 안전한 범위**로 잡는다
- 세게 칠 때 물리가 부정확해지는 것은 납득 가능한 대가가 아니다

## 7.5 이벤트 (확정)

`Step()`이 이벤트 리스트를 반환한다.

- `WallBounce(puckId)`
- `PuckCollision(a, b, impulse)`
- `PuckDestroyed(puckId)`

경험치 계산, 데미지 처리, 뷰의 사운드·이펙트가 모두 같은 소스를 읽는다.
뷰가 시뮬 결과를 눈으로 짐작해 타이밍을 맞추는 상황을 피한다.

## 7.6 폴더

```
Assets/Scripts/Sim    시뮬레이션 (UnityEngine 의존은 Vector2만)
Assets/Scripts/View   렌더링, 입력
```

## 7.7 현재 구현 상태

**완료된 것**

- `Assets/Scripts/Sim` — 결정론적 헤드리스 2D 퍽 시뮬레이션
  - Puck: Id, Position, Velocity, Radius, Mass, Owner, BounceCount, Health
  - PuckSim: 퍽 리스트 + 사각형 보드 경계, `Step()`
  - 등감속 마찰 (임계값 이하 0으로 스냅)
  - 벽 반사 + BounceCount 증가
  - 원-원 충돌, 법선 방향 임펄스, 반발계수 파라미터
  - `Simulate(puckId, initialVelocity)` — 전부 정지할 때까지 헤드리스 실행
  - `Clone()` — 전체 상태 깊은 복사
  - 고정 타임스텝 1/120, 무작위성 없음, UnityEngine 의존은 Vector2뿐
  - `Step()`이 이벤트 리스트 반환 — `WallBounce(id)`, `PuckCollision(a, b, impulse)`. 한 번 할당한 버퍼를 Clear해 재사용
  - 서브스텝 적분 — 터널링 방지 (마찰은 스텝당 1회 유지해 정지거리 불변)
  - 충돌·벽 해소를 Id 오름차순으로 — 리스트 순서(파괴·재생성) 무관
  - 물리 파라미터 설정 클래스 `PuckSimConfig` (마찰·반발·정지 임계값·벽 감쇠·충돌 감쇠), `RemovePuck`/`SetHealth`
  - 게임 규칙 일부: 스톤 체력, 팀이 다른 충돌 시 서로 -1(같은 팀 무피해), 체력 0 파괴 + `PuckDestroyed` 이벤트(그 스텝 물리는 먼저 적용)
  - 칸 점유 판정 `BoardCells` (원-사각 정밀, `반지름-최근접거리≥임계값`), 안쪽 3x3 버프 / 바깥 데미지 분류
    - **임계값은 상수 0.1로 고정**(View의 `OccupancyThreshold`). 반지름(1.5) 이상이면 식이 성립할 수 없어
      칸 점유가 통째로 꺼진다(하이라이트·버프·데미지 칸 전부 무효) — 슬라이더로 노출하지 말 것
  - 버프 칸 `BuffKind`(공격/쉴드, 체커보드)·`BuffValue`(중앙 2 / 나머지 안쪽 1)·`SumBuffs`(점유 칸 합산, 공격·쉴드 분리)
  - 경험치·레벨: 벽 반사(소유자 무관)·아군 충돌로 +1, 3당 레벨업(상한 5, 만렙 이후 버림)
  - 데미지 칸 정산 `SettleDamageCells`: 주어진 스톤의 점유 데미지 칸 수 × 데미지 체력 감소, 0 이하 파괴.
    **이벤트도 반환값도 없다** — 정산으로 죽은 스톤은 호출 측이 대상 Id를 정산 뒤 재조회해 찾아야 한다
  - 적 AI 탐색 `EnemyPlanner` — 규칙이 아니라 시뮬 탐색. 후보 격자(보드 스톤 × 방향 × 파워, 진입 자리 × 부채꼴 × 파워)를
    각각 `Clone()`으로 헤드리스 실행 → 점수화(버프 확보·상대 피해·자기 피해·데미지 칸) → 순위로 선택. 무작위성 없음
    - **예측 핸디캡**: 기본은 플레이어 동급(미리보기처럼 큐가 친 스톤을 롤아웃에서 제거 → 산란·캐스케이드는 AI도 모름).
      완전 예측은 최고 난이도 전용 — 8장 4번("결과를 모른다")을 AI가 우회하지 않게 하는 장치
    - 난이도 5단계는 조준 격자 밀도 + 고르는 샷의 순위로 만든다. **실수는 무작위가 아니라 순위 기반**이라 결정론 유지
- 검증 코드 21종
  - 결정론 / 정지거리 / 운동량 보존
  - 이벤트 결정론 / 이벤트 발생 / 터널링 없음 / Id 순서 독립성
  - 벽 감쇠 / 충돌 감쇠 / RemovePuck / SetHealth
  - 교차 팀 데미지 / 체력 0 파괴 / 칸 점유 / 칸 분류 / 경험치 획득 / 레벨 상한 / 데미지 칸 정산
  - 버프 칸 종류 / 버프 합산 / 적 AI(결정론·합법 수)

**View** — 플레이테스트(`Assets/Scripts/View`, `Assets/Editor/PuckmitePlaytestMenu`)에서 **한 판의 게임 루프가 닫혀 있다.**
빈 보드에서 시작 → 새 스톤 투입 또는 큐샷 → 정지 후 버프 확정 → 공격 → 적 AI가 같은 절차를 자동 수행 → 승패.

- 런타임 생성 보드·벽·퍽(씬 파일 없음, 아트는 절차적 자리표시 — 나중에 애셋 교체), 배속, HUD 튜닝 슬라이더(창 높이에 맞춰 스크롤)
- **조준**: 당구식 당겨 쏘기(가고 싶은 방향의 반대로 당김), 궤적 미리보기 + 도착 마커, 우클릭 취소.
  조준선/파워 선은 미리보기가 대체해서 제거했다 — 다시 넣지 말 것
- **스톤 표시**: 체력 호·레벨 숫자(TMP)·경험치 원형 게이지, 파괴, 칸 점유 하이라이트
- **턴**: 개별 행위자 순서, 강제 굴리기 1회, 사망 행위자 스킵, 턴 시작 데미지 칸 정산.
  굴릴 게 없는 턴은 스킵하지 않고 잠시 보여준 뒤 기본 공격으로 끝낸다
- **새 스톤 투입**: 손(사용가능/대기) 관리, 진입 변 고스트(변을 따라서만 이동), 겹치면 발사 차단.
  파괴된 스톤은 **Id를 재사용**해 손으로 돌아온다 — 뷰 배열이 Id로 인덱싱되므로 새 Id를 발급하면 안 된다
- **캐릭터**: 위쪽에 행위자 4명(체력·공격·쉴드·보유 스톤), 버프칸 공격/쉴드 색 구분
- **공격**: 마우스를 올린 적에 하이라이트 → 클릭해 대상 지정, 효과 쉴드 → 기본 쉴드 → 체력 순 소모, 승패 배너
- **적 AI**: 자동 진행(생각 시간 뒤 발사), 난이도 5단계, ON/OFF 토글로 핫시트 복귀
- **하이라이트 규약**: 노랑 = 지금 조작 가능 / 빨강 = 지금 떼면 발사 / 반투명 = 선택 가능한 대상 / 회색 = 자리 막힘.
  잡기 판정은 **그려진 링 안쪽까지만**이다(보이는 것과 눌리는 것을 일치시킨다)

턴·행위자·캐릭터 전투는 모두 뷰 전용 진행 로직 — Sim 불변

**아직 없는 것** — 상점·진행 구조(스테이지/런)·보스·적 종류별 차별화·밸런스 확정

## 7.8 개발 순서

한 번에 한 단계씩 진행한다. 단계를 건너뛰거나 묶지 않는다.

**Sim 단계**

1. ✅ **완료** — `Step()`이 이벤트 리스트 반환 — `WallBounce`, `PuckCollision`
   (리스트는 한 번 할당하고 Clear해서 재사용)
2. 🟡 **일부 완료** — Sim 안정화 — Id 오름차순 충돌 해결 ✅, 서브스텝(터널링 방지) ✅,
   `Simulate()` 최대 스텝 캡(`RunToRest` 기존 유지), 물리 파라미터 설정 클래스 분리(보류 — 디버그 패널 착수 때)

**View 단계**

> 참고: 다중 퍽 플레이테스트(`PuckmitePlaytest`)가 3~5를 놀이터 형태로 한꺼번에 덮었다.
> 손맛 확인(6)을 앞당기려고 순서를 벗어나 먼저 만들었다. 정식 분리 구현은 손맛이 잡힌 뒤 정리.

3. 보드 그리기 — 직교 카메라, 5x5 격자, 안쪽 3x3 구분, 벽 경계.
   런타임 절차 생성, 씬 파일 만들지 않음
4. 퍽 하나 띄우기 — 퍽 렌더링(Owner 색 구분, Id·BounceCount 표시),
   고정 스텝 어큐뮬레이터 드라이버, 배속 컨트롤
5. 클릭·드래그 발사 — 방향과 파워 동시 입력, 조준선, 파워 게이지.
   발사 속도는 한 번만 계산해서 재사용
6. 🟢 **디버그 패널 완료 + 손맛 임시값 확정** — 마찰·정지 임계값·반발계수·충돌 감쇠·벽 감쇠·최대 파워·파워 커브 슬라이더.
   임시 손맛값(놀이터 기본값): 마찰 10, 반발 1.0, 충돌 감쇠 0.7, 정지 임계값 0.4, 벽 감쇠 0.6, 파워 스케일 6.0, 최대 파워 50, 파워 커브 1.0 (선형).
   "일단은" 값이며 최종 밸런스에서 재조정 (충돌 감쇠 = 퍽끼리 부딪힐 때 속도 손실, restitution과 별개)
7. 🟢 **완료** — 궤적 미리보기 — Clone 후 헤드리스 실행, 큐 스톤만 최종 정지까지 추적,
   큐가 친 스톤은 제거해 캐스케이드 무시, 예상 도착 마커 표시, 계산 시간 측정 (놀이터: 드래그 중 실시간)

**전투 단계** (손맛 확인 후 진행한 것 — 순서는 그때그때 정했다)

8. 🟢 **완료** — 버프 칸 + 위쪽 캐릭터 — 안쪽 9칸 공격/쉴드 구분, 행위자 4명 스탯 표시.
   버프는 **자기 턴 종료에 확정해 다음 자기 턴까지 유지**(효과 쉴드 방식), 버프량은 칸값 × 스톤 레벨
9. 🟢 **완료** — 캐릭터 공격 (턴 4단계) — 강제 공격 1회·대상 1명, 쉴드 먼저 소모, 승패 판정
10. 🟢 **완료** — 새 스톤 투입 — 손 관리, 진입 변 고스트, 빈 보드에서 시작
11. 🟢 **완료** — 적 AI — 시뮬 탐색 + 난이도 5단계, 예측 능력은 플레이어와 동급이 기본

**지금 여기 — 기능을 먼저 다 만들고, 밸런스는 맨 마지막에 한다.**

12. 진행 구조 — 스테이지/런, 적 수 증가, 런 단위 초기화
13. 상점 — 칸 구매·스톤 구매·리롤, 골드
14. 보스
15. 적 종류별 차별화
16. **밸런스 확정** — 모든 임시 수치를 여기서 한 번에 잡는다

> **순서 변경 기록.** 원래 이 문서는 "상점은 전투가 재미있다는 것이 확인되기 전까지 착수하지 않는다"로
> 밸런스를 상점 앞의 관문에 두었다. 게임을 먼저 완성하고 밸런스를 마지막에 몰아서 잡기로 방침을 바꿨다.
> 대신 감수하는 것: 재미 검증 없이 그 위에 상점·보스를 쌓게 되므로, 밸런스 단계에서 수치가 아니라
> **구조를 바꿔야 한다는 결론**이 나오면 그때 되돌리는 비용이 커진다.

현재 수치는 전부 임시값이다: 스톤 체력 5, 칸 데미지 1, 버프 중앙 2 / 나머지 1,
플레이어 체력 20·공격 2·쉴드 0, 적 체력 10·공격 1·쉴드 0, 플레이어 시작 스톤 2 / 적 1.
16단계까지 이 값들을 건드리지 않고, 어색한 점은 그때그때 기록만 해둔다.

## 7.9 View 작업 시 제약

- `.unity` 씬 파일을 만들거나 수정하지 않는다.
  런타임에 전부 생성하는 부트스트랩 MonoBehaviour 하나만 만들고,
  빈 GameObject에 붙이는 것은 사람이 한다
- 외부 아트 에셋을 쓰지 않는다. 메시·스프라이트·텍스처는 절차적으로 생성한다
- 렌더링은 시뮬 상태를 **읽기만** 한다. 발사 호출 외에는 쓰기 금지
- 배속은 프레임당 소비하는 1/120 스텝 개수만 바꾼다.
  시뮬레이션 결과를 바꿔서는 안 된다
- 미리보기는 Clone한 사본에서만 실행한다. 실제 시뮬을 절대 변경하지 않는다

## 7.10 단계별 수용 기준

각 단계 완료 시 확인한다.

- 같은 보드 상태에서 같은 샷을 두 번 → 최종 위치가 완전히 동일
- x1 재생과 즉시 재생의 최종 상태가 동일 (뷰가 시뮬을 오염시키지 않았는지)
- 미리보기 끝점이 첫 충돌 시점의 실제 큐 퍽 위치와 정확히 일치
- 보드 반대편에서 최대 파워로 정지한 퍽을 정면 사격 → 반드시 충돌 (터널링 없음)
- 모든 샷이 스텝 캡에 걸리기 전에 종료

---

# 8. 해결된 난제 — 파워와 정확도의 트레이드오프

이전 문서에서 최우선 난제로 두었던 문제다.
"세게 칠수록 벽 반사 경험치가 쌓이는데 대가가 없어서 최적 전략이 최대 파워로 수렴한다."

**아래 규칙들의 조합으로 해결되었다.** 별도 시스템을 추가하지 않았다.

1. **판이 무너진다** — 세게 치면 내가 세워둔 스톤들이 흩어진다.
   흩어진 스톤은 바깥 데미지 칸에 멈추고, 다음 턴에 체력을 잃고, 파괴되면 **레벨이 0으로 초기화**된다.
   경험치를 벌려고 세게 칠수록 이미 벌어놓은 경험치가 날아간다
2. **상대도 같이 큰다** — 벽 반사 경험치는 소유자 무관이므로,
   판을 흩뜨리면 **상대 스톤도 함께 레벨업**한다
3. **혼돈은 수가 많은 쪽에 유리하다** — 양쪽 스탯이 0에 수렴하면
   나는 한 대 때리고 세 대 맞는다
4. **결과를 모른다** — 미리보기가 큐 스톤만 보여준다.
   최대 파워 샷은 "나는 안전한데 판이 어떻게 될지 모르는" 도박이다
5. **만렙이 가깝다** — 레벨 상한 5, 필요 경험치 3 고정.
   최대 12 경험치면 만렙이므로 한 샷에 3~4번 튕기면 두세 턴 만에 찬다

5번의 결과로 **런마다 두 국면이 생긴다.**
초반은 세게 쳐서 레벨을 올리는 국면, 만렙 이후는 세게 칠 이유가 사라진 정밀 배치 국면이다.
"무한 파밍 가능"은 허용하되, 실질적으로는 초반 2~3턴짜리 문제로 축소된다.

---

# 9. 설계 시 주의할 지점

해결된 것은 아니고, 수치를 정할 때 반드시 고려해야 하는 구조적 특성이다.

## 9.1 교차점이 중앙보다 강할 수 있다

겹친 칸을 전부 100% 적용하므로, 스톤을 **4칸 교차점**에 세우면 버프칸 4개를 동시에 받는다.
3x3 버프 구역 안의 내부 교차점은 4곳이다.
반면 가장 강한 중앙칸 한가운데에 정확히 세우면 1칸만 받는다.

**"안쪽으로 갈수록 강해진다"는 설계 의도가 수치에 따라 무력화될 수 있다.**
중앙칸 값을 바깥 4칸 합보다 확실히 크게 잡거나, 등급 격차를 크게 벌려야 한다.
스톤 지름이 3이라 교차점 조준 난이도가 그리 높지 않다는 점도 감안해야 한다.

## 9.2 보스전에서 턴 압력이 역전된다

런 4는 적 3명이라 내가 1번 치는 동안 3번 맞는다. 파밍할 여유가 없다.
런 5는 보스 1마리라 **라운드당 공격이 1번**이다. 턴 압력이 가장 낮다.

즉 파밍이 가장 이득인 상황이 하필 가장 여유로운 싸움이라,
"오래 파밍하다가 마지막에 정리"가 최적해가 될 수 있다.
보스 데미지를 일반 적의 3배 이상으로 잡거나, 보스 고유 행동으로 턴 압력을 만들어야 한다.
보스에게 스톤 3개를 준 것은 이 관점에서 맞는 방향이다.

## 9.3 데미지 칸 중첩의 폭발력

바깥 교차점에 서면 데미지 칸 3개에 동시에 걸려 **3배 데미지**를 받는다.
스톤 체력을 정할 때 이 최악 케이스를 기준으로 잡아야 한다.
체력이 낮으면 벽 근처에서 스톤이 순식간에 증발한다.

---

# 10. 미정 — 정하기 전까지 구현하지 않는다

> **임시로 정한 것과 아직 안 정한 것을 구분한다.**
> "놀이터 임시"라고 적힌 항목은 동작을 보려고 값을 넣어둔 것이지 확정이 아니다. 밸런스 국면에서 재조정한다.
> 표시가 없는 항목은 정말로 안 정한 것이므로 임의로 채우지 말고 물어본다.

## 10.1 수치 전반

- 스톤의 체력 수치 (놀이터 임시 5, 슬라이더 1~8로 튜닝 — 확정 아님)
- 데미지 칸이 한 번에 깎는 양 (놀이터 임시 1, 슬라이더 1~5 — 확정 아님)
- 3x3 버프칸의 **등급 구조와 수치** (안쪽일수록 강하다는 방향만 확정.
  놀이터 임시: 공격/쉴드를 체커보드로 배치, 중앙 2 / 나머지 안쪽 1 — 확정 아님)
- 공격력·쉴드·데미지의 **계산식**
- 레벨업 시 칸 효과에 더해지는 가산량 (놀이터 임시: **칸값 × 스톤 레벨** — 가산이 아니라 곱이다. 확정 아님)
- 기본 공격력, 기본 쉴드, 최대 체력의 기본값
  (놀이터 임시: 플레이어 체력 20·공격 2·쉴드 0, 적 체력 10·공격 1·쉴드 0 — 확정 아님)
- 시작 스톤 개수 (플레이어 2는 3.3에서 확정. 적은 놀이터 임시 1개씩 — 확정 아님)
- 런 종료 시 회복량의 기본값
- 적 종류별 스탯과 스톤 개수
- 보스 스탯
- 적 처치 시 골드 획득량
- 칸 가격, 스톤 가격, 리롤 가격 곡선
- 상점 칸 4종 각각의 강화량
- 겹침 판정 임계값의 최종 수치 (놀이터 임시 0.1 — 확정 아님.
  다만 **반지름 미만이어야 한다**는 상한은 구조적 제약이다. 3.2 참고)
- 마찰 감속량, 벽 반사 감속량, 파워 상한 (임시 손맛값은 7.8 6단계에 기록 — 확정 아님, 밸런스에서 재조정)

## 10.2 시스템

- 🟡 **적 AI 전반** — 접근법은 정했다(아래). 성격 부여와 최종 난이도 곡선은 미정.
  - 우려했던 "결정론 시뮬이라 AI가 최적해를 찾는다"는 문제는 **예측 능력을 플레이어와 맞춰서** 풀었다.
    AI도 미리보기와 똑같이 큐 스톤만 예측하고, 부딪힌 스톤의 산란은 모른다 (6.2 / 8장 4번).
    완전 예측은 최고 난이도에서만 연다
  - 난이도는 **조준 격자 밀도 + 고르는 샷의 순위**로 만든다.
    조준 오차나 실수를 무작위로 넣지 않는다 — 순위 기반이라 결정론이 유지된다
  - 평가 함수는 임시다: 버프 확보 · 상대 스톤 피해와 파괴 · 자기 피해 · 데미지 칸 정지의 가중합
- ✅ **조준 UI의 두 모드 전환** — 전환을 없앴다. 손에 스톤이 있으면 진입 변에 항상 고스트가 떠 있고,
  고스트를 당기면 새 스톤, 보드 위 스톤을 당기면 큐샷이다. 별도 모드 버튼 없음
- **보스 고유 패턴** — 스테이지별 행동과 칸 배치 변경. 보스 구현 시점에 정한다
- 칸 종류를 4종에서 늘릴지 여부

---

# 11. 보류 — 초기 구상에 있었으나 현재 설계에 없음

되살릴지 말지 아직 결정하지 않은 것들이다.

- **마나와 특수공격**
- **전투 보드 커스터마이징** (반사벽 배치 등)
- **적의 스킬** (칸 효과를 봉인하는 등 스톤 굴리기 외의 행동)
- **스톤의 3축 스탯** (무게 / 크기 / 탄성)
  — 레벨 상한 5와 겹침 판정으로 스톤의 개성이 레벨 하나로 수렴했다. 되살릴 여지가 크지 않다
- **스톤 특성 시스템** (개별 스톤에 귀속되는 규칙 변형)
- **전투 후 보상 선택** (셋 중 하나 고르기)
- **맵과 갈림길 선택** (현재는 3스테이지 x 5런의 고정 진행)
- **메인 메뉴**
- **이어하기**

## 명시적으로 제거된 것

- **공격속도 스탯** — 턴 순서를 결정하는 데 쓰려 했으나 제거됨.
  턴은 `플레이어 → 적1 → 적2 → 적3` 고정이다
- **경험치 최소 충돌 속도 조건** — 설명 비용이 크다는 이유로 도입하지 않기로 함
- **상점 칸의 위치별 가격 차등** — 도입하지 않음
- **방어력 매 턴 전면 재계산** — 쉴드는 기본/효과 두 종류로 분리되었다.
  기본 쉴드는 런 단위, 효과 쉴드는 턴 단위다
- **패스** — 굴리기도 공격도 강제다
